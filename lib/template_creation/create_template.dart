import 'package:flutter/material.dart';

class TemplateCreationScreen extends StatelessWidget {
  const TemplateCreationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.redAccent, child: const Text('Okay'));
  }
}
